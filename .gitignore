# Node / TypeScript
node_modules/
.DS_Store

# Provider config (local to each user)
provider.json

# Pi session + auth state (local to each user)
.pi/sessions/
.pi/auth.json


# Python bytecode (retained for any user-side scripts)
__pycache__/
*.pyc



# Visual-asset generation workspace
.vhs-work/

# Scratch dev dir (reference/) — NOT docs/reference/, which ships the
# bundled knowledge base. Anchored with a leading slash to scope to repo root.
/reference/

# Misc
*.log
debug_payload.json
brainstorm_outputs/

# Project memory (keep FORMAT.md, ignore others)
Memory/
!Memory/FORMAT.md
/sessions/

# Local planning/development artifacts (never committed)
.agents/
